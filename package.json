{
  "name": "chatbots-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered chat web application",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c teal,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "vercel-build": "npm run build --prefix frontend",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run db:push --prefix backend",
    "prisma:generate": "npm run prisma:generate --prefix backend"
  },
  "devDependencies": {
    "@vercel/node": "^5.3.6",
    "concurrently": "^9.1.2"
  }
}
